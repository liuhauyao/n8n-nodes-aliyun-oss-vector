# 通过 GitHub Actions 发布到 npm 并生成 provenance（2026-05-01 起 Creator Portal 验证要求）
# 参考：https://docs.n8n.io/integrations/community-nodes/build-community-nodes/
#
# 需在 GitHub 仓库设置 Secrets：NPM_TOKEN（npm Granular Token，具备该包 publish 权限）
# 或通过 npm Trusted Publisher（OIDC）绑定本仓库与本 workflow，可不使用长期 token（见 npm 文档）
#
# · push 到 main 或打 tag（*.*.* / v*.*.*）：verify 通过后自动 npm publish（需 NPM_TOKEN）
# · PR 对准 main：仅 verify（不发布）
# · 手动 Run workflow：verify 通过后发布（用于重试或紧急发版）
# 若 registry 上已有同版本，npm publish 会失败，需先 bump package.json 版本再推送 main。

name: Publish

on:
  push:
    branches:
      - main
    tags:
      - '*.*.*' # 如 0.2.37（npm version 若配置 tagVersionPrefix=""）
      - 'v*.*.*' # 如 v0.2.37（npm version 默认）
  pull_request:
    branches:
      - main
  # 配置好 NPM_TOKEN 后，可在 Actions 里手动「Run workflow」重试发布（无需再打 tag）
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify (n8n community ESLint)
        run: npm run verify

  publish:
    name: Publish to npm
    needs: verify
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')))
    runs-on: ubuntu-latest
    concurrency:
      group: npm-publish-${{ github.repository }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Require NPM_TOKEN secret
        run: |
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then
            echo "::error::未配置仓库 Secret「NPM_TOKEN」。请到 Settings → Secrets and variables → Actions → New repository secret，填入 npm Granular Token（需具备本包 publish 权限）。"
            exit 1
          fi

      - name: Install dependencies
        run: npm ci

      - name: Publish to npm with provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance --access public
